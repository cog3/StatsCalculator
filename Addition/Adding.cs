using System;
namespace Addition
{
    public class Adding : IAdd
    {
        private dynamic Result;
        public dynamic Sum(dynamic a, dynamic b)
        {
            Result = Sum(a, b);
            return Result;
        }
        public dynamic Sum(dynamic arrayList)
        {
            Result = Sum(arrayList);
            return Result;
        }
    }
}
